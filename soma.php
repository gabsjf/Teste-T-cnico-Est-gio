<?php

$indice = 13;
$soma = 0;
$k = 0;

while ($k < $indice) {
    $k = $k + 1;
    $soma = $soma + $k;
}

echo $soma; // Imprime a variável SOMA

//Após a execução, o valor da variável soma será 91, pois o código calcula a soma dos numeros iterados de 1 a 13.
?>

